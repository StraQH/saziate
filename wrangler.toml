name = "saziate"
main = "worker.ts"
compatibility_date = "2025-07-01"
compatibility_flags = ["nodejs_compat"]

# Assets directory produced by @opennextjs/cloudflare build
assets = { directory = ".open-next/assets", binding = "ASSETS" }

[observability.logs]
enabled = true
invocation_logs = true


[[d1_databases]]
binding = "DB"
database_name = "saziate-db"
database_id = "92932312-77ce-422a-8cd7-be1e32472897" 

[vars]
NEXT_PUBLIC_APP_NAME = "Saziate"
NEXT_PUBLIC_MOCK_MODE = "true"

[build]
command = "npm run build:worker"

[triggers]
crons = [
  "0 0 1 * *",  # Monthly: generate-invoices (Run on 1st of month at midnight)
  "0 0 * * *",  # Daily: process-payouts (Run every night at midnight)
  "*/5 * * * *" # Every 5 minutes: dispatch-notifications (Process email/SMS queue)
]

